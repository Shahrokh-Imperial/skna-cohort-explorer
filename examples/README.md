# Synthetic cohort example

Use the three bundled files to test the interface. They are generated data and do not reproduce manuscript results. In the sidebar, point the signal source to this directory and choose `example_events.csv` and `example_thresholds.csv`.
