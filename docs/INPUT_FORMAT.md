# Input format

A processed recording requires a numeric time column and median SKNA column. The manuscript convention uses `time_s` and `skna_med`; channel-specific SKNA and retained physiological columns are optional. The events table needs recording ID, onset, and end. The threshold table needs recording ID and `selected_threshold_uV`.
