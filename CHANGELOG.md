# Changelog

## 1.0.0 — first public release

- Removed caches and manuscript-specific metadata.
- Added synthetic example, tests, user and input-format documentation.
- Preserved a synchronized local copy of the read-only cohort I/O and analysis helpers so the GUI can run independently.
